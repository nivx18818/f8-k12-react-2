const config = {
  routes: {
    home: "/",
    products: "/products",
    productDetail: "/products/:slug",
    login: "/login",
    users: "/users",
    notFound: "*",
  },
};

export default config;
