const config = {
  routes: {
    home: "/",
    products: "/products",
    productDetail: "/products/:slug",
    login: "/login",
    register: "/register",
    users: "/users",
    notFound: "*",
  },
};

export default config;
